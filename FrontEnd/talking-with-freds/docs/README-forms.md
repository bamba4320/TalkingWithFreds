#Forms
- Pay attention to the fill `<input>`s with `defined` value!
<br>When this error happens to you, ☝🏼 this is the reason why
```
Warning: A component is changing an uncontrolled input of type tel to be controlled. Input elements should not switch from uncontrolled to controlled (or vice versa). Decide between using a controlled or uncontrolled input element for the lifetime of the component. More info: https://fb.me/react-controlled-components
```
- Use the `loading` prop in Semantic-ui `Form` in order to stop the user from making multiple request to the server
- Use `onSubmit` prop in `<Form>`
- Use `htmlFor` in labels