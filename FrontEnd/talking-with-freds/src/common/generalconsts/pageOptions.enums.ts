export enum MessagesPageOptions {
	Context = 'MessagesContext',
}

export enum LayoutPageOptions {
	withoutHeader = 'withoutHeader',
	withoutFooter = 'withoutFooter',
	withSticky = 'withSticky',
	hasError = 'hasError',
}

export enum NewsletterPageOptions {
	withoutBusiness = 'withoutBusiness',
}

export enum ApplicationPageOptions {
	mobxStores = 'mobxStores',
	mobileDetect = 'mobileDetect',
	userAgentDetector = 'userAgentDetector',
}
