export enum HowToSend {
	email = 1,
	sms = 2,
	share = 3,
}

export enum ToWhom {
	toMyself = 1,
	toOther = 2,
}

export enum WhenToSend {
	now = 1,
	other = 2,
}

export enum PageToRender {
	sendGift = 0,
	payment = 1,
	submit = 2,
}

export enum eStatusOfUse {
	unUsed = 0,
	used = 1,
	pariallyUsed = 2,
	expired = 3,
}
