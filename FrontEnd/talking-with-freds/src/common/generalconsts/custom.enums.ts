export enum IconType {
	exit = 'x',
	prev = 'angle right',
}

export enum LinkType {
	primary = 'primary',
	regular = 'regular',
	navbarLink = 'navbarLink',
}

export enum TagsContext {
	HOME_PAGE = 'Home Page',
	FOR_WORKERS = 'Employee gift',
	VALIDATE_PURCHASE = 'Home Page',
	LOWER_SLIDER = 'Lower Slider',
	UPPER_SLIDER = 'Upper Slider',
}
